name: Check Status After DB Fix

on:
  workflow_dispatch:

jobs:
  check-status:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check current status after database fix
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_KEY }}
        script: |
          cd /var/www/www-root/data/www/rualtech.ru/
          
          echo "🔍 === ПРОВЕРКА ПОСЛЕ ИСПРАВЛЕНИЯ БД ==="
          
          echo "📋 Текущие настройки БД:"
          grep "DB_" .env
          
          echo "🗄️ Тест подключения к БД:"
          php artisan tinker --execute="try { DB::connection()->getPdo(); echo 'DB подключение OK!'; } catch(Exception \$e) { echo 'DB Error: '.\$e->getMessage(); }"
          
          echo "📝 Последние ошибки Laravel (после исправления):"
          if [ -f "storage/logs/laravel.log" ]; then
            echo "--- Последние 15 строк ---"
            tail -15 storage/logs/laravel.log
          else
            echo "❌ Лог файл не найден"
          fi
          
          echo "🔧 Проверяем миграции:"
          php artisan migrate:status || echo "❌ Проблема с миграциями"
          
          echo "🧹 Очищаем кеш еще раз:"
          php artisan config:clear
          php artisan cache:clear
          php artisan view:clear
