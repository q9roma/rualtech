name: Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_KEY }}
        script: |
          echo "🔍 Диагностика деплоя"
          echo "📂 Текущая директория:"
          pwd
          
          cd /var/www/www-root/data/www/rualtech.ru
          echo "📂 Перешли в директорию проекта:"
          pwd
          
          echo "📊 Статус Git перед обновлением:"
          git status || echo "Git status failed"
          
          echo "📋 Последний коммит:"
          git log --oneline -1 || echo "Git log failed"
          
          # Исправляем права доступа для Git
          echo "🔧 Исправляем права доступа Git..."
          git config --global --add safe.directory /var/www/www-root/data/www/rualtech.ru
          git config --global --add safe.directory '*'
          
          # Принудительно получаем обновления
          echo "📥 Получаем обновления из GitHub..."
          git fetch origin main --force
          echo "🔄 Сбрасываем до последнего коммита..."
          git reset --hard origin/main
          git clean -fd
          
          echo "📊 Статус Git после обновления:"
          git status || echo "Git status failed"
          
          echo "📋 Коммит после обновления:"
          git log --oneline -1 || echo "Git log failed"
          
          echo "🔍 Проверяем содержимое файла home.blade.php:"
          if [ -f "resources/views/frontend/home.blade.php" ]; then
            grep -n "Алтех\|Altech" resources/views/frontend/home.blade.php || echo "Не найдено ни Altech, ни Алтех"
          else
            echo "❌ Файл home.blade.php не найден!"
          fi
          
          # Очищаем кэши
          echo "🧹 Очищаем кэши Laravel..."
          php artisan cache:clear 2>/dev/null || echo "Cache clear failed"
          php artisan view:clear 2>/dev/null || echo "View clear failed"
          php artisan config:clear 2>/dev/null || echo "Config clear failed"
          
          # Перезапускаем веб-сервер
          echo "🔄 Перезапускаем веб-сервер..."
          systemctl reload nginx 2>/dev/null || echo "Nginx reload failed"
          
          echo "✅ Деплой завершён! Проверьте сайт."
